var app = new Framework7({
    // App root element
    root: '#app',
    // App Name
    name: '${config.appName}',
    // App id
    id: 'com.ezappx.${config.appName}',
    // Enable swipe panel
    panel: {
        swipe: 'left',
    },
});